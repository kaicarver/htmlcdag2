<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home.ejs</title>
</head>
<body>
    <h1>Bienvenue sur Home.ejs</h1>
    <% data.forEach((contact) => { %>
        <div>
            <a href="formulaire/<%= contact._id %>"><%= contact.firstname %></a>
        </div>
    <% }); %>
</body>
</html>